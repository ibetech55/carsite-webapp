import { MilesFormatterPipe } from './miles-formatter-pipe';

describe('MilesFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new MilesFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
