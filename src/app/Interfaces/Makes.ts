export interface IMakes {
  makeName: string;
  origin: number;
  active:boolean;
  yearFounded:number;
  makeCode:string;
  company:string;
}

export interface IMakeList {
    makeName:string;
    makeCode:string;
}